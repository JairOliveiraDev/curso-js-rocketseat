// for...in
// cria um objeto 
let person = {
    name: 'John',
    age: 30,
    weight: 88
}

//loop mostrando todas as propriedades desse objeto
for(let property in person) {
    console.log(person);
}